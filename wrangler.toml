name = "snaptext-relay"
main = "worker/index.js"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[[durable_objects.bindings]]
name = "ROOMS"
class_name = "Room"

[[migrations]]
tag = "v1"
new_classes = ["Room"]

[build]
command = ""

# For Cloudflare Pages + Workers integration
# Deploy worker separately, then set VITE_WS_URL in Pages env
